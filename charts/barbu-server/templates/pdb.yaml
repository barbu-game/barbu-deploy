apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  # Voluntary disruptions (node drain, rolling update) never take the service to zero.
  minAvailable: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
