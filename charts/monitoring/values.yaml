postgres:
  host: barbu-postgres-rw.barbu.svc
  database: barbu
  appSecret: barbu-db-app   # owner creds, used by the init Job to grant
namespace: monitoring

alertmanager:
  discordSecret: alertmanager-discord   # key 'url' = Discord webhook
